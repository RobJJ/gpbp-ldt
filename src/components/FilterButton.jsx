// start as server component -> not sure if needs to rather be client... lets see

export default function FilterButton() {
  return (
    <div className="w-full bg-blue-300 flex justify-center p-2">
      FilterButton
    </div>
  );
}
